name: WireGuard Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 🧰 Установка WireGuard
        run: |
          set -x
          sudo apt update
          sudo apt install -y wireguard curl

      - name: 🌐 Проверка интернета ДО VPN
        run: |
          set -x
          echo "📡 IP до VPN:"
          echo "ipinfo.io:" && curl https://ipinfo.io/ip || echo "❌"
          echo "ifconfig.me:" && curl https://ifconfig.me || echo "❌"
          echo "api.myip.com:" && curl https://api.myip.com || echo "❌"

          echo ""
          echo "📶 Пинг до IP VPN сервера (может быть заблокирован):"
          ping -c 4 45.77.30.118 || echo "❌ Сервер не отвечает на ping (это допустимо)"

      - name: 📝 Создание WireGuard конфигурации
        run: |
          set -x
          sudo mkdir -p /etc/wireguard
          echo "[Interface]" | sudo tee /etc/wireguard/wg0.conf
          echo "PrivateKey = gAyGrUttLdgh+ENPy7os9vFLycJgn6PC3XuANg6RAWg=" | sudo tee -a /etc/wireguard/wg0.conf
          echo "Address = 10.1.0.2/32" | sudo tee -a /etc/wireguard/wg0.conf
          echo "DNS = 1.1.1.1" | sudo tee -a /etc/wireguard/wg0.conf
          echo "" | sudo tee -a /etc/wireguard/wg0.conf
          echo "[Peer]" | sudo tee -a /etc/wireguard/wg0.conf
          echo "PublicKey = /2MM4ThCUi3SEckHKqduAK/I09gMXoPSQIQR0H63ZAw=" | sudo tee -a /etc/wireguard/wg0.conf
          echo "AllowedIPs = 0.0.0.0/0, ::/0" | sudo tee -a /etc/wireguard/wg0.conf
          echo "Endpoint = 45.77.30.118:51821" | sudo tee -a /etc/wireguard/wg0.conf
          echo "PersistentKeepalive = 25" | sudo tee -a /etc/wireguard/wg0.conf

      - name: 🔌 Подключение к VPN
        run: |
          set -x
          sudo wg-quick up wg0
          echo "🎯 WireGuard статус:"
          sudo wg show

      - name: 🌍 Проверка интернета ЧЕРЕЗ VPN
        run: |
          set -x
          echo "nameserver 1.1.1.1" | sudo tee /etc/resolv.conf
          sleep 3
          echo "🌐 IP через VPN:"
          echo "ipinfo.io:" && curl https://ipinfo.io/ip || echo "❌"
          echo "ifconfig.me:" && curl https://ifconfig.me || echo "❌"
          echo "api.myip.com:" && curl https://api.myip.com || echo "❌"

      - name: ✅ Ping через VPN
        run: |
          set -x
          echo "📶 Ping до 1.1.1.1 через VPN:"
          ping -c 4 1.1.1.1 || echo "❌ Ping не прошёл"

      - name: 📴 Отключение VPN
        if: always()
        run: |
          set -x
          sudo wg-quick down wg0
